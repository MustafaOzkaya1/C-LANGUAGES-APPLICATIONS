#include <iostream> 
using namespace std;
int main () {
int a,b,c,d,e,f,g,h,i,j;
string karar= "evet";
string input; 
         
     cout<<"1.sayiyi giriniz" <<endl;
	 cin>>a;
	  cout<<"2.sayiyi giriniz" <<endl;
	 cin>>b;
	  cout<<"3.sayiyi giriniz" <<endl;
	 cin>>c;
	  cout<<"4.sayiyi giriniz" <<endl;
	 cin>>d;
	  cout<<"5.sayiyi giriniz" <<endl;
	 cin>>e;
	  cout<<"6.sayiyi giriniz" <<endl;
	 cin>>f;
	  cout<<"7.sayiyi giriniz" <<endl;
	 cin>>g;
	  cout<<"8.sayiyi giriniz" <<endl;
	 cin>>h;
	  cout<<"9.sayiyi giriniz" <<endl;
	 cin>>i;
	  cout<<"10.sayiyi giriniz" <<endl;
	 cin>>j;
	

		if(a>b && a>c && a>d && a>e && a>f && a>g && a>h && a>i && a>j) {
		cout<<"en buyuk sayi: "<<a<<endl;
		}
		else if (j>b && j>c && j>d && j>e && j>f && j>g && j>h && j>i && j>a) {
		cout<<"en buyuk sayi: "<<j<<endl;
		}
		else if (i>b && i>c && i>d && i>e && i>f && i>g && i>h && i>j && i>a) {
		cout<<"en buyuk sayi: "<<i<<endl;

		}
		else if (h>b && h>c && h>d && h>e && h>f && h>g && h>i && h>j && h>a ) {
		cout<<"en buyuk sayi: "<<h<<endl;

		}
		else if (g>b && g>c && g>d && g>e && g>f && g>h && g>i && g>j && g>a) {
		cout<<"en buyuk sayi: "<<g<<endl;

		}
		else if (f>b && f>c && f>d && f>e && f>g && f>h && f>i && f>j && f>a) {
		cout<<"en buyuk sayi: "<<f<<endl;

		}
		else if (e>b && e>c && e>d && e>f && e>g && e>h && e>i && e>j && e>a) {
		cout<<"en buyuk sayi: "<<e<<endl;

		}
		else if (d>b && d>c && d>e && d>f && d>g && d>h && d>i && d>j && d>a) {
		cout<<"en buyuk sayi: "<<d<<endl;

		}
		else if (b>d && b>c && b>e && b>f && b>g && b>h && b>i && b>j && b>a) {
		cout<<"en buyuk sayi: "<<b<<endl;

	}
	 
	 
	    
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
}
